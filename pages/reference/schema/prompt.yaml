source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/works/prompt.md
description: Schema reference for  works prompt
title: Schema -  works prompt
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Works']
      state: publish
      type: post
