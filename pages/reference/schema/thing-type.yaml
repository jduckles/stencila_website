source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/other/thing-type.md
description: Schema reference for  other thing-type
title: Schema -  other thing-type
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Other']
      state: publish
      type: post
