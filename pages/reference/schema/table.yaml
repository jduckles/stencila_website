source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/works/table.md
description: Schema reference for  works table
title: Schema -  works table
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Works']
      state: publish
      type: post
