source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/other/brand.md
description: Schema reference for  other brand
title: Schema -  other brand
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Other']
      state: publish
      type: post
