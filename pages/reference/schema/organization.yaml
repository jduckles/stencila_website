source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/other/organization.md
description: Schema reference for  other organization
title: Schema -  other organization
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Other']
      state: publish
      type: post
