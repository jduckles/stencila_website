source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/works/directory.md
description: Schema reference for  works directory
title: Schema -  works directory
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Works']
      state: publish
      type: post
