source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/other/contact-point.md
description: Schema reference for  other contact-point
title: Schema -  other contact-point
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Other']
      state: publish
      type: post
