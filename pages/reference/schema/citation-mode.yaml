source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/prose/citation-mode.md
description: Schema reference for  prose citation-mode
title: Schema -  prose citation-mode
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Prose']
      state: publish
      type: post
