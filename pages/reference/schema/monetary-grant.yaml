source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/other/monetary-grant.md
description: Schema reference for  other monetary-grant
title: Schema -  other monetary-grant
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Other']
      state: publish
      type: post
