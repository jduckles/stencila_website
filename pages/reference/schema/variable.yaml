source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/flow/variable.md
description: Schema reference for  flow variable
title: Schema -  flow variable
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Flow']
      state: publish
      type: post
