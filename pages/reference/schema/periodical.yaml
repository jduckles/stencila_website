source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/works/periodical.md
description: Schema reference for  works periodical
title: Schema -  works periodical
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Works']
      state: publish
      type: post
