source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/data/string-hint.md
description: Schema reference for  data string-hint
title: Schema -  data string-hint
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Data']
      state: publish
      type: post
