source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/other/role.md
description: Schema reference for  other role
title: Schema -  other role
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Other']
      state: publish
      type: post
