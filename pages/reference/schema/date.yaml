source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/data/date.md
description: Schema reference for  data date
title: Schema -  data date
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Data']
      state: publish
      type: post
