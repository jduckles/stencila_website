source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/works/claim.md
description: Schema reference for  works claim
title: Schema -  works claim
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Works']
      state: publish
      type: post
