source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/prose/list-item.md
description: Schema reference for  prose list-item
title: Schema -  prose list-item
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Prose']
      state: publish
      type: post
