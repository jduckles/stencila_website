source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/prose/section.md
description: Schema reference for  prose section
title: Schema -  prose section
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Prose']
      state: publish
      type: post
