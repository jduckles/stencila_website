source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/data/date-time.md
description: Schema reference for  data date-time
title: Schema -  data date-time
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Data']
      state: publish
      type: post
