source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/other/raw-block.md
description: Schema reference for  other raw-block
title: Schema -  other raw-block
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Other']
      state: publish
      type: post
