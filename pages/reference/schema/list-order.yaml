source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/prose/list-order.md
description: Schema reference for  prose list-order
title: Schema -  prose list-order
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Prose']
      state: publish
      type: post
