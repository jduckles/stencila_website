source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/other/message-level.md
description: Schema reference for  other message-level
title: Schema -  other message-level
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Other']
      state: publish
      type: post
