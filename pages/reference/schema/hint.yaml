source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/data/hint.md
description: Schema reference for  data hint
title: Schema -  data hint
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Data']
      state: publish
      type: post
