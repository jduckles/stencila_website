source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/other/model-parameters.md
description: Schema reference for  other model-parameters
title: Schema -  other model-parameters
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Other']
      state: publish
      type: post
