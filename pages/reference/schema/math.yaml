source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/math/math.md
description: Schema reference for  math math
title: Schema -  math math
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Math']
      state: publish
      type: post
