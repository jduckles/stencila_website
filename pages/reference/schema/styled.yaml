source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/style/styled.md
description: Schema reference for  style styled
title: Schema -  style styled
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Style']
      state: publish
      type: post
