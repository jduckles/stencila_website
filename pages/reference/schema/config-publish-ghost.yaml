source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/config/config-publish-ghost.md
description: Schema reference for  config config-publish-ghost
title: Schema -  config config-publish-ghost
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Config']
      state: publish
      type: post
