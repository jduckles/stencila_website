source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/other/provenance-count.md
description: Schema reference for  other provenance-count
title: Schema -  other provenance-count
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Other']
      state: publish
      type: post
