source: https://raw.githubusercontent.com/stencila/stencila/refs/heads/main/docs/reference/schema/other/thing.md
description: Schema reference for  other thing
title: Schema -  other thing
config:
  publish:
    ghost:
      tags: ['#schema', '#doc', 'Other']
      state: publish
      type: post
